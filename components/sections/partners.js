import { Row, Col } from "../flexbox";
import SectionHeading from "../SectionHeading";

export default () => (
  <section id="partners">
    <SectionHeading>파트너</SectionHeading>
    <Row>
      <Col xs={1} />
    </Row>
  </section>
)
